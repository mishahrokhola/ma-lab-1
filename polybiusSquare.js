const {messageMatrix} = require("./_messageMatrix");

function polybiusSquare(message) {
    // form 2D array from message
    const matrix = messageMatrix(message, 6, 6);

    return ''
}

exports.polybiusSquare = polybiusSquare
